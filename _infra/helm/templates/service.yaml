apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
    type: NodePort
    ports:
        - port: 80
          targetPort: 8094
          protocol: TCP
    selector:
        app: {{ .Chart.Name }}
