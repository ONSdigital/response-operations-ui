replicaCount: 1

namespace: minikube

service:
  port: 8094
  type: ClusterIP

ingress:
  enabled: false
  host: rops.example.com
  certName: frontstage-cert

uaa:
  client_id: response_operations
  client_secret: password

networkPolicy:
  egress: |
    - {}
  ingress: |
    - {}

resources:
  requests:
    memory: "256Mi"